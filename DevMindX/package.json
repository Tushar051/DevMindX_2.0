{
  "name": "devmindx",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix front",
    "build": "npm run build --prefix MindCoder",
    "start": "npm run start --prefix MindCoder"
  },
  "dependencies": {
    "mongodb": "^6.18.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^25.3.0",
    "@types/nodemailer": "^6.4.17"
  }
}
