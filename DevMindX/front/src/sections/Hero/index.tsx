import { HeroTitle } from "@/sections/Hero/components/HeroTitle";

export const Hero = () => {
    return (
        <section id="home" className="relative box-border caret-transparent pt-[140px] pb-[60px] md:pt-[190px]">
            <HeroTitle />
        </section>
    );
};
